name: YRGN CI

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Build and deploy
      run: |
        ssh-add - <<< ${{ secrets.SSH_PRIVATE_KEY }}
